class Solution {
public:  
    void moveZeroes(vector<int>& nums) {  
       int len = nums.size();
       int  id = 0;
       for(int i = 0;i<len;i++){
        if(nums[i])
          nums[id++] = nums[i];
       }
       for(int j = id;j<len;j++ )
        nums[j] = 0;
    }  
};
